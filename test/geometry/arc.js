import * as assert from "assert";

describe("Geometry", function () {

  describe("arc", function () {
    it("needs actual tests", function () {
      assert.strictEqual(
        true,
        true
      );
    });
  });
});
